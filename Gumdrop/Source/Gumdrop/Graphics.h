#include "Component.h"

class Graphics : public Component
{
	/*
		The graphics class is the basis to rendering items on screen, from an Actor, to UI. the class will contain definitions for how to render things to the screen
		As stated in previous classes, because this is something you could choose to add to an actor (could be a UI Actor or otherwise) it needs to inherit from component
	*/
};
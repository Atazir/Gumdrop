#include "Graphics.h"

class UI : Graphics
{
	/*
	Any UI objects needing to be rendered to the screen, from fancy animations to text to images and buttons will be rendered to the screen. The class needs to inherit from Graphics because it is 
	a core component to rendering UI elements
	*/

};